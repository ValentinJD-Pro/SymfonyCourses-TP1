{% extends 'base.html.twig' %}

{% block title %}Allergen{% endblock %}

{% block body %}
    <h1 class=titleAdmin>Allergen</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ allergen.id }}</td>
                <td>{{ allergen.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="adminBtn" href="{{ path('allergen_index') }}">back to list</a>

    <a class="adminBtn" href="{{ path('allergen_edit', {'id': allergen.id}) }}">edit</a>
<div class="adminDel">
    {{ include('allergen/_delete_form.html.twig') }}</div>

    <style>
        .table td,.table th{
            width:calc(100%/3);
        }
    </style>
{% endblock %}
